package cps161_FinalProject;
public class FunkyBoard {
    private char[][] board;
    private int boardSize;

    public FunkyBoard(int size) {
        boardSize = size;
        board = new char[boardSize][boardSize];
        for (int i = 0; i < boardSize; i++) {
            for (int j = 0; j < boardSize; j++) {
                board[i][j] = '-';
            }
        }
    }

    public int getBoardSize() {
        return boardSize;
    }

    public char getTokenAt(int x, int y) {
        // Implement getTokenAt logic
    }

    public void setTokenAt(int x, int y, char token) {
        // Implement setTokenAt logic
    }

    public void displayBoard() {
        // Implement displayBoard logic
    }

    public FunkyToken getWinner() {
        // Implement getWinner logic
        for (FunkyToken token : tokens) {
            if (token.hasOwnedSpaces(this)) {
                return token;
            }
        }
        return null;
    }

    public void placeToken(FunkyToken token, int x, int y) {
        // Implement placeToken logic
    }
}
